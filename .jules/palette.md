## 2024-05-22 - Confirmation modal for Clear Cart
**Learning:** Destructive actions like clearing the cart need explicit confirmation from the user to prevent accidental data loss.
**Action:** Wrapped `cartNotifier.clearCart()` in a confirmation dialog when adding UX improvements.
